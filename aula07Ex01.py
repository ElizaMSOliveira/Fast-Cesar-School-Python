"""Usando arrays com numpy faça média das
 notas de uma pessoa estudante"""

import numpy as np

notas = []
notas.append()
for i in range(0, 3):
    notas = float(input(f'Informa a {i} Nota do Estudante: '))
    notas.append(notas)

    """
    import numpy as np

notas_list = []

for c in range (1, 4):
    notas = (float(input("Informe as 3 notas do estudante: ")))
    notas_list.append(notas)


notas_array = np.array(notas_list)
media = notas_array.mean()
print(f"A média das notas = {media:.2f}")
    """



